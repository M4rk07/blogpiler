<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container-fluid">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <img src="{{ asset('img/ic-menu.png') }}" onclick="openNav()" class="b-nav-icon b-section-clickable"/>
        {% endif %}
        <a href="{{ path('homepage') }}"><img src="{{ asset('img/bnlogo-darkred.png') }}" class="b-nav-logo"/></a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fa fa-bars"></i>
        </button>
        <div class="container-fluid">
            <div class="col-lg-12">
                <form role="search">
                    <div class="input-group">
                        <input type="text" class="form-control" placeholder="Search" name="q">
                    </div>
                </form>
            </div>
        </div>
        <a href="{{ path('homepage') }}"><img src="{{ asset('img/ic-home.png') }}" class="b-nav-icon"/></a>
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
            <button class="b-btn b-btn-default b-btn-default-filled" onclick="showLoginForm()">Log In</button>
        {% endif %}
    </div>
</nav>

{% if is_granted('IS_AUTHENTICATED_FULLY') %}

    <div id="mySidenav" class="sidenav">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <span class="b-sidebar-title">you</span>
                    <ul class="list-group">
                        <li class="list-group-item b-sidebar-item"><a href="{{ path('profile', { 'rUserId': app.user.userId }) }}">Profile</a></li>
                    </ul>
                </div>
            </div>
            <hr>
            <div class="row">
                <div class="col-lg-12">
                    <span class="b-sidebar-title">shortcuts</span>
                    <ul class="list-group">
                        <li class="list-group-item b-sidebar-item"><a href="{{ path('blog') }}">Mudja</a></li>
                    </ul>
                </div>
            </div>
            <hr>
            <div class="row">
                <div class="col-lg-12">
                    <span class="b-sidebar-title">other</span>
                    <ul class="list-group">
                        <li class="list-group-item b-sidebar-item"><a href="{{ path('logout') }}">Log Out</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

{% endif %}

{% if not is_granted('IS_AUTHENTICATED_FULLY') %}

<!-- Modal -->
<div class="modal fade" id="loginModal" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <ul class="nav nav-tabs" role="tablist" id="accTab">
                    <li class="nav-item b-tab-item"><a class="nav-link active" data-toggle="tab" role="tab" href="#login" id="loginTab">Log In</a></li>
                    <li class="nav-item b-tab-item"><a class="nav-link" data-toggle="tab" role="tab" href="#register" id="registrationTab">Register</a></li>
                </ul>
                <button type="button" class="close b-section-clickable" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="container">

                    {% include 'parts/login-registration.html.twig' %}

                </div>
            </div>
        </div>
    </div>
</div>

{% endif %}